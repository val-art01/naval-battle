/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java application project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/8.0.2/userguide/building_java_projects.html
 */

plugins {
    // Apply the application plugin to add support for building a CLI application in Java.
    id 'application'
    id 'org.openjfx.javafxplugin' version '0.0.13'
//    id 'org.openjfx.javafxplugin' version '0.0.10'
//    id 'org.testfx.tesjunit5' version '3.0.0-alpha4'
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
    // Use JUnit Jupiter for testing.
    testImplementation 'org.junit.jupiter:junit-jupiter:5.9.1'
    testImplementation 'org.testfx:testfx-junit5:4.0.16-alpha'

    // This dependency is used by the application.
    implementation 'com.google.guava:guava:31.1-jre'

    // https://mvnrepository.com/artifact/org.openjfx/javafx
    implementation 'org.openjfx:javafx:19.0.2'

//    testImplementation 'org.testfx:junit-jupiter:4.0.16-alpha'
}

application {
    // Define the main class for the application.
    mainClass = 'mains.App'
}

tasks.named('test') {
    // Use JUnit Platform for unit tests.
    useJUnitPlatform()
}

tasks.named('compileJava') {
    // use the project's version or define one directly
    options.javaModuleVersion = provider { version }
}

javafx{
    version = '19.0.+'
    modules = [
        'javafx.controls',
        'javafx.fxml',
    ]
}

//test {
//    jvmArgs = ['--module-path', 'path/to/javafx/lib', '--add-modules', 'javafx.controls']
//}
